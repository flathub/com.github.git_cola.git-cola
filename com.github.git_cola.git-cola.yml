app-id: com.github.git_cola.git-cola
runtime: org.kde.Platform
runtime-version: 5.15-22.08
sdk: org.kde.Sdk
base: com.riverbankcomputing.PyQt.BaseApp
base-version: 5.15-22.08
command: git-cola
rename-desktop-file: git-cola.desktop
rename-icon: git-cola
rename-appdata-file: git-cola.appdata.xml
finish-args:
  - --share=ipc
  - --share=network
  - --socket=wayland
  - --socket=fallback-x11
  - --filesystem=home
  - --filesystem=xdg-data/Trash
  - --socket=ssh-auth

modules:
  - python3-qtpy.json
  - name: git-cola
    buildsystem: simple
    build-commands:
      - python3 -m build --wheel --no-isolation
      - pip3 install --prefix=${FLATPAK_DEST} dist/*.whl
    sources:
      - type: git
        url: https://github.com/git-cola/git-cola.git
        tag: v4.0.4
        commit: 19f615f22d371de9b0d08a2d29642b2d40b7dcf7
        x-checker-data:
          type: git
          tag-pattern: ^v([\d.]+)$

  - tcl.json
  - tk.json
  - git.json
